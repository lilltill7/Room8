# Room8 — Ideas List

Features and improvements to build when ready.

---

## UI / Personalization

- **User color scheme toggle** — let users switch between light, dark, and system default themes from within the app (instead of relying solely on OS settings). Could live in Profile settings.

---

## Security — Required Before Real Launch

- **JWT authentication** — replace the current localStorage user_id system with signed tokens. Currently anyone with DevTools can impersonate any user. Fine for a closed pilot with known test users, but must be fixed before pitching to universities or launching publicly. Estimated 2-3 hours of work.

---
